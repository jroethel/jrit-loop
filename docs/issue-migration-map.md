# Issue migration map: loop-stack-session to jrit-loop

This table is the old-number-to-new-URL record that gets filled in as the sixteen Task 11 transfers run, because sixteen renumbered issues with no record is sixteen dead cross-references in every brief, plan, and commit message that ever cited them.

| Old # | Old title                                                                                                                                         | New URL                            |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| 7     | Case for Matt's triage / tdd / prototype skills                                                                                                   | `pending: filled at transfer time` |
| 15    | github-to-gitlab data migration                                                                                                                   | `pending: filled at transfer time` |
| 18    | Reconcile the 'who routes, and on what basis' seam across loop-drive, ringer, and the model-intel reference                                       | `pending: filled at transfer time` |
| 28    | A `/dev/tty`-hardened enforcement gate, if the cooperative-agent assumption ever breaks and an agent-proof import gate is actually wanted         | `pending: filled at transfer time` |
| 40    | Second amendment direction - human rejection of a bad PASS as a reclassify value, the mirror image barthballard named, schema-ready but unshipped | `pending: filled at transfer time` |
| 48    | loop-drive pre-run Rubix pass                                                                                                                     | `pending: filled at transfer time` |
| 51    | Model routing: task_type has no definition, no per-shape model-fit signal, and Agent-tool transport never feeds the evidence loop                 | `pending: filled at transfer time` |
| 56    | Where does architecture brainstorming/research fit in the loop-stack process?                                                                     | `pending: filled at transfer time` |
| 45    | Session coordination: check-before-modify + ownership handoff between concurrent sessions                                                         | `pending: filled at transfer time` |
| 33    | RIT-team turnkey phase with no OS assumption                                                                                                      | `pending: filled at transfer time` |
| 61    | Fable retirement path - gate the claude-md managed block behind a host flag and split fable-guidelines into model-agnostic and Fable-era content  | `pending: filled at transfer time` |
| 62    | Role-pin single home - consolidate the three resolves-to-Opus pin declarations into one routing home with pointers                                | `pending: filled at transfer time` |
| 63    | Worked examples for the orchestrator pin trigger - add one positive and one rejected pin example under the routing chain's step 3                 | `pending: filled at transfer time` |
| 64    | herdr plugin surface - a herdr plugin manifest pane hosting a drive panel or board view                                                           | `pending: filled at transfer time` |
| 36    | Decision E remainder: standing glossary (CONTEXT.md) + ADR side-effects from loop-brainstorm                                                      | `pending: filled at transfer time` |
| 3     | Quota-aware scheduling / unattended auto-resume (third parking)                                                                                   | `pending: filled at transfer time` |
