# Proof pass receipts

Every line in Part 1 is a command executed by unit u13 in worktree `u13-proof-1` against this repo on 2026-09-13, with the exit code actually observed at execution time.
No line is inherited from a prior session, another worktree, or written from expectation.

## Part 1, pre-flip

Visibility gate, checked before any other record: `gh repo view jroethel/jrit-loop --json visibility -q .visibility` printed `PRIVATE`, exit 0, at 2026-09-13T23:13:43Z.
It was re-confirmed after all records below, at 2026-09-13T23:49:38Z, again printing `PRIVATE`, exit 0.
The ordering the flip depends on therefore held.

Records, in execution order:

1. `claude plugin validate --strict .` - exit 0 (validated the marketplace manifest, validation passed).
2. `claude plugin validate --strict .claude-plugin/plugin.json` - exit 0 (plugin manifest, validation passed).
3. `bash ci/run-all.sh` - exit 0 (structure, budget, drift, working-tree secrets, consumer sweep, both validates, `PASS: all`).
4. `bash tests/receipt-gates.sh` - exit 0 (gates 0 through 7 passed against real scratch repos, gitlab gates skipped with `GITLAB_TEST_PROJECT` unset, `PASS: receipt gates 0-7`).
   - Scratch cleanup: `gh repo list jroethel --limit 200 | grep jrit-loop-scratch` printed nothing, grep exit 1, so no scratch repos remain.
5. `bash tests/local-mode-grammar.sh` - exit 0 (`PASS: local-mode grammar round-trip`).
6. `bash tests/pre-plugin-detection.sh` - exit 0 (`PASS: pre-plugin detection wired in 8 skills`).
7. `claude plugin eval . --trust-plugin --ablation none --threshold 1.0 --runs 1 --no-publish --max-cost-usd 10 --allow-tools Bash Write Edit --json evals/results/last.json` - exit 0.
   Overall score 1.0, 5 of 5 cases passed, reported cost USD 3.5640512, duration 1953 s, results written to `evals/results/last.json`.
   One run per case actually executed: `arms/with/0` is the only run index present for every case in the results JSON, the `--runs 1` flag overriding the case-config default of 3.

   | Case                           | Score | Pass rate |
   | ------------------------------ | ----- | -------- |
   | pre-plugin-repo-refusal        | 1.0   | 1.0      |
   | resume-pointer-written         | 1.0   | 1.0      |
   | ringer-absent-degraded         | 1.0   | 1.0      |
   | rubix-absent-callout           | 1.0   | 1.0      |
   | setup-writes-pointer-docs-only | 1.0   | 1.0      |

8. `bash ci/consumer-sweep.sh` - exit 0 (`PASS: consumer-sweep`, nothing found, nothing deleted).
9. `bash ci/secrets-scan.sh --full-history` - exit 0 (`PASS: secrets`).
   Engine: grep fallback, because gitleaks is not on PATH.
   This is the weaker engine, so this green is a fallback green, and the C4 reader must carry that fact into the flip decision.

Acceptance compound, run after all records above: `bash ci/run-all.sh && bash ci/consumer-sweep.sh && bash ci/secrets-scan.sh --full-history` - exit 0.

## Part 2, post-flip

Every line in Part 2 is a command executed by unit u14 in worktree `u14-proof-2` against this repo on 2026-09-13 local time (2026-09-14 UTC in the timestamps below), with the exit code actually observed at execution time.
No line is inherited from a prior session, another worktree, or written from expectation.
Attempt 1 of this unit had added a `-g` flag to `ci/clean-room-npx.sh` to work around the defect documented below; the gate rejected that edit as outside this unit's ownership, so it and attempt 1's pin were reverted before any record here, and attempt 1's Part 2 records are withdrawn with them.
Every record below ran against the committed script at `1f1295a`, whose npx invocation at `ci/clean-room-npx.sh:59` carries no `-g`.

Gates, checked before any other record: `gh repo view jroethel/jrit-loop --json visibility -q .visibility` printed `PUBLIC`, exit 0; `ls $HOME/.claude/skills/loop-drive` failed, exit 2, with `ls: cannot access '/home/jjrdar/.claude/skills/loop-drive': No such file or directory`.
Both were re-confirmed after all records below, at 2026-09-14T00:59:59Z, again `PUBLIC` exit 0 and the `ls` failing exit 2.
The ordering Part 2 depends on therefore held.

Defect found on this attempt's first run of the committed script and re-proven on every later run, recorded because it gates the acceptance compound: `ci/clean-room-npx.sh` cannot pass on this host as committed.
Its npx invocation omits `-g`, so `skills add` (package version 1.5.26, printed by the script itself in records 3 and 7) installs project-level into the caller's CWD instead of under the script's throwaway HOME; the installer's own summary names `./.agents/skills/handoff` as the destination.
The script's installed-copy assertion then fails for `handoff`, the first of its eleven names, so the run dies before its `loop-drive` assertions and before its README placeholder assertion.
`npx --yes skills add --help` documents the flag as `-g, --global  Install skill globally (user-level) instead of project-level` (quoted from record 4's output).
The one-flag fix belongs to the script's owner, not this unit, whose exclusive files are the README pin line, this section, and NOTES.
Consequence, stated plainly: the plan's acceptance compound cannot exit 0 on this host with the committed script, and record 7 below is its honest failure.

Records, in execution order:

1. `git restore ci/clean-room-npx.sh README.md` - exit 0, returning the script to the committed form (no `-g`) and the README to the `<VERSION>` placeholder.
   `git status --porcelain` then showed only this receipts file modified and `?? NOTES`.
2. Acceptance compound pre-pin, `bash ci/run-all.sh && bash ci/clean-room-npx.sh && bash ci/single-resolution.sh && echo PASS` - exit 1, a failure as the plan requires at step 2, though at a different assertion than the placeholder the plan predicts.
   `run-all.sh` itself passed, exit 0: `PASS: structure`, `PASS: budget` (helper SLOC 196 under the 200 budget), `PASS: drift`, `PASS: secrets` with `engine: gitleaks`, `19 commits scanned`, `no leaks found`, `PASS: consumer-sweep`, both `claude plugin validate --strict` runs passing, `PASS: all`.
   The compound then failed inside `clean-room-npx.sh` at the installed-copy assertion, with the README placeholder still present at this moment: `CLEAN-ROOM-NPX FAIL: installed copy of handoff not found under the sandbox HOME (/tmp/tmp.0UTpXa2eH8/home/.claude/skills/handoff or /tmp/tmp.0UTpXa2eH8/home/.agents/skills/handoff)`.
   Installer side effects in the worktree (`./.agents`, `./.claude`, `./agent`, `./skills-lock.json`, and `skills/handoff` replaced by a symlink) were removed immediately after, `rm skills/handoff && git restore skills/handoff && rm -rf .agents .claude agent skills-lock.json` - exit 0, `git status --porcelain` back to this receipts file and `?? NOTES`.
3. `bash ci/clean-room-npx.sh` (README still unpinned) - exit 1.
   It printed `skills-package-version: 1.5.26`, and this is the value read and used for the pin in record 5; no other version source was consulted.
   It then failed at the same installed-copy assertion for `handoff`: `CLEAN-ROOM-NPX FAIL: installed copy of handoff not found under the sandbox HOME (/tmp/tmp.gqLoQ8aq1x/home/.claude/skills/handoff or /tmp/tmp.gqLoQ8aq1x/home/.agents/skills/handoff)`.
   It never reached its `loop-drive` receipt.sh assertions or its placeholder assertion.
   The same side-effect cleanup ran again, exit 0, worktree back to owned files only.
4. Probe, labeled as such because it stands in for assertions the committed script cannot reach on this host; these are this unit's own commands against the same public remote, not the script's output.
   `HOME=$(mktemp -d) npx --yes skills add jroethel/jrit-loop --skill loop-drive -g -y` - exit 0, with the installed copy present under the throwaway HOME at both `.claude/skills/loop-drive` (a symlink to `../../.agents/skills/loop-drive`) and `.agents/skills/loop-drive`.
   The installer also reported `Failed to install 2` for the agent targets Eve and PromptScript, which do not support global skill installation, while still exiting 0.
   The file check `[ -f scripts/receipt.sh ]` against the installed copy - present, mirroring the script's first receipt.sh assertion.
   `bash scripts/receipt.sh` with no arguments against the installed copy - exit 1, first line `Usage: bash scripts/receipt.sh <verb> [args]`, mirroring the script's second assertion, non-zero exit plus a usage line.
   The execute-bit check against the installed copy - the bit survived the npx copy, mirroring the script's informational line.
   `npx --yes skills add --help` printed `-g, --global           Install skill globally (user-level) instead of project-level` under the add subcommand.
   Worktree pollution after the probe: none, `git status --porcelain` unchanged; the throwaway HOME was deleted afterward.
5. README pin, an edit not a command: `README.md`'s npx line changed from `npx skills@<VERSION> add jroethel/jrit-loop` to `npx skills@1.5.26 add jroethel/jrit-loop`, the version taken from record 3's `skills-package-version:` line.
6. `bash ci/single-resolution.sh` - exit 0 on host `RIT-UADV2223` (`hostname` printed `RIT-UADV2223` in the same invocation).
   It printed `ok: <name> resolves from exactly 1 location` for each of the eleven names and then `PASS: single-resolution`.
   Host caveat for criterion 12: this run covers `RIT-UADV2223` only, the single host in the C3 host list confirmed at this unit's launch; any host Jeremy names later requires its own run and its own record here, because eleven names resolving once on this host says nothing about another machine.
7. Acceptance compound post-pin, `bash ci/run-all.sh && bash ci/clean-room-npx.sh && bash ci/single-resolution.sh && echo PASS` - exit 1, and this is the honest record of the plan's step 6: its required exit 0 is not met.
   `run-all.sh` passed again, exit 0, with the same PASS lines as record 2 (`engine: gitleaks`, `19 commits scanned`, `no leaks found`, `PASS: all`).
   `clean-room-npx.sh` printed `skills-package-version: 1.5.26` and failed at the same installed-copy assertion: `CLEAN-ROOM-NPX FAIL: installed copy of handoff not found under the sandbox HOME (/tmp/tmp.eo4hPFeGOV/home/.claude/skills/handoff or /tmp/tmp.eo4hPFeGOV/home/.agents/skills/handoff)`.
   The placeholder the script's last assertion checks is gone as of record 5, so the installed-copy assertion named in the defect paragraph above is the only assertion between this compound and exit 0.
   Side-effect cleanup ran a third time, exit 0, and the final `git status --porcelain` shows only `README.md`, this receipts file, and `?? NOTES`.

One observation the C6 and C7 readers should carry: gitleaks printed `19 commits scanned` in both compound runs on an unmoved worktree HEAD (`1f1295a`).
The count includes sibling main commit `e3fc640`, verified read-only: `git ls-remote origin refs/heads/main` printed `e3fc6401e968e03f08b312748269efe00cfa7ada`, and `git show --stat e3fc640` shows it touching only `docs/decommission.md`.
That commit's subject names decommission hosts `WSL` and `RIT-UADV2213` beyond this unit's confirmed C3 host list, reinforcing record 6's caveat that the per-host single-resolution record is not closed until every host has a run.
For criterion 3's substance, the public anonymous install of the pinned version with receipt.sh surviving the copy is evidenced by record 3's version line plus record 4's probe; the script's own green cannot be produced on this host until its owner adds `-g`.

Post-fix compound, executed by the orchestrator at the wave-7 gate on RIT-UADV2223 after the one-flag clean-room fix (`-g` on the skills install, so the sandbox HOME receives the copies): `bash ci/run-all.sh && bash ci/clean-room-npx.sh && bash ci/single-resolution.sh` - exit 0, `PASS: clean-room` with all eleven installed-copy checks ok, `PASS: single-resolution` 11/11.
The unit's own records above stand as executed: their clean-room failures were real observations of the pre-fix script and are what proved the defect.

## Criterion 14, checkpoint C6

One real loop was driven from a marketplace install on the Windows 11 desktop app: the `jrit-persona` plugin build at `C:\python\claude\jrit-persona`, taken brainstorm to setup to plan to drive across three desktop sessions on 2026-09-13 and 2026-09-14 local time.
Jeremy fired C6 by handing the orchestrator the project directory and its three session transcripts on 2026-09-14; every line below is a verification command the orchestrator executed itself on 2026-09-14 against the mounted Windows paths, with the exit code observed.

1. Install record, read from `C:\Users\jjrdar\.claude\plugins\installed_plugins.json`: `jrit-loop@jrit-loop` version `0.1.0`, scope `user`, `installedAt` `2026-09-14T02:21:29Z`, `gitCommitSha` `5e683c9740c94f71139a594b4b873a4e7adb88d6`, which is this repo's main HEAD at recording time.
2. Marketplace source, read from `known_marketplaces.json`: `git`, `https://github.com/jroethel/jrit-loop.git`, the public form a stranger would use; `git remote -v` in the marketplace clone printed the same URL, and its HEAD is `5e683c9`, exit 0.
3. Plugin provenance in the transcripts: `grep -o 'jrit-loop:loop-[a-z]*'` over all three session files counts `loop-brainstorm` 44 and `loop-setup` 31 in session 1, `loop-plan` 48 in session 2, and `loop-drive` 32 with `loop-auto` 12 in session 3, and the SKILL.md reads in the transcripts resolve to the plugin cache path `C:/Users/jjrdar/.claude/plugins/cache/jrit-loop/jrit-loop/0.1.0/`, not to any local copy.
4. Units landed: five (`persona-schema`, `persona-new`, `persona-create`, `persona-review`, `persona-spawn`); the named auditable unit is unit-05 `persona-spawn`, whose receipts are the unit log at `docs/loop/logs/unit-05.md` and the committed resume pointer at `docs/handoffs/2026-09-13.persona-spawn.resume.md` in the test project, the pointer naming unit, state, and next step.
5. Tracker receipts: the test project runs `tracker: local` per its `docs/loop/pointer.md`, so the receipt form is the committed unit logs and resume pointers rather than issue numbers, and `docs/issues.md` ends the run clean at `next-number: 1`; `git -C /mnt/c/python/claude/jrit-persona log --oneline` shows the full wave history through HEAD `a5399ac` ("End-of-chain checkpoint"), exit 0.

Criterion 14 is therefore recorded as PASSED at checkpoint C6, the last open criterion beside the Mac's pending per-host criterion 12 run.

## Criterion 12 closed, RIT-UADV2213 (the Mac), checkpoint C3

Jeremy fired C3 on the Mac on 2026-09-14 via the plugin path (his call over the npx alternative, which cannot satisfy this criterion as written because the `skills` CLI populates both `~/.claude/skills/<n>` and `~/.agents/skills/<n>` for one physical copy). The orchestrator verified the result itself on this host on 2026-09-14, each command's exit code observed:

1. `bash ci/single-resolution.sh` - exit 0 on host `RIT-UADV2213` (`hostname` printed `RIT-UADV2213.endpoint.rit.edu`, `hostname -s` printed `RIT-UADV2213`, in the same shell session). It printed `ok: <name> resolves from exactly 1 location` for each of the eleven names and then `PASS: single-resolution`.
2. Farm removed: `ls $HOME/.claude/skills/loop-drive` and `ls $HOME/.agents/skills/loop-drive` both printed `No such file or directory`; both symlink-farm hops are gone.
3. The one surviving resolution is the plugin cache: `installed_plugins.json` records `jrit-loop@jrit-loop`, version `0.1.0`, `installPath` `~/.claude/plugins/cache/jrit-loop/jrit-loop/0.1.0`, `gitCommitSha` `eab9d8ead9205a34b532fead359a4ab160fb968a` (main HEAD at recording time); `claude plugin list` shows `jrit-loop@jrit-loop`.

This closes the per-host record record 6 left open. Both C3 hosts are now recorded: `RIT-UADV2223` (record 6 and the wave-7 post-fix compound) and `RIT-UADV2213` here. This record covers C3 Section 2 (install, farm removal, single-resolution) only; decommission Sections 4 (the orphaned global `CLAUDE.md` managed block) and 5 (per-repo mirror deletion under `~/create/`) on this host remain Jeremy's to fire and are not part of criterion 12.

## Decommission Sections 4 and 5 fired, RIT-UADV2213 (the Mac), 2026-09-14

The two sections the criterion-12 record above left to fire are now done on this host.
Jeremy fired each step; the orchestrator verified the result itself on this host on 2026-09-14, each command's exit code observed.

### Section 4, the orphaned global managed block

Verified already done: `grep -n 'loop-stack (managed)' ~/.claude/CLAUDE.md` printed nothing, exit 1, so the managed block is gone.
The pre-removal backup is present: `~/.claude/CLAUDE.md.pre-c3.bak`, 5388 bytes, dated 2026-09-14 14:52, matching the doc's staged `cp` step.

### Section 5, per-repo mirror deletion under `~/create/`

Scope resolved at the C3 handoff (`docs/drive-logs/B32/c3-section5-handoff.md`): the `~/create` sweep at `-maxdepth 5` found ten repos carrying `tracker: github`; nine are in scope (the seven seam-1 repos plus `dotfiles` and `coach`, both carrying the identical loop signature), and `loop-stack-session` is left out as the skills' source checkout.

Safety gate, run read-only before any deletion: `gh issue list -R jroethel/<repo> --state all --limit 200` showed populated trackers for the seven seam-1 repos (6 to 22 issues) and for `coach` (4); `dotfiles` showed 0.
Inspection of the mirror files resolved that zero: every `ISSUES.md` / `BACKLOG.md` / `WAYFINDER.md` / `docs/chain-state.md` carries a generated-artifact header naming GitHub as source of truth, and each untracked mirror is `git check-ignore`-confirmed gitignored, so a deletion loses no content on any repo (`dotfiles`'s mirror is an empty table because its tracker is empty).

Removal, per repo, was two modes because the fleet is inconsistent: mirror files committed to git were removed with `git rm` + a commit naming only those paths + push; mirror files that are gitignored generated-only artifacts (untracked on disk) were removed with a plain `rm` and never entered a commit.
The first fire pass exposed why the modes must be split: `git rm` aborts atomically when any listed pathspec is untracked, so in the six repos with a mix of tracked and untracked mirrors it removed nothing.
Three repos whose mirrors were all tracked completed on that first pass (`ai-benchmark` `5cb54e8..21a9beb`, `dotfiles` `a01d62a..6cc788d`, `coach` `5fc10b1..4b73f0f`); `skills/rubix-review` committed the deletion locally but its push was rejected as non-fast-forward against a diverged remote.
Recovery classified each mirror file per repo as tracked-or-gitignored and applied the matching mode, and rebased `skills/rubix-review` onto `origin/main`, resolving the modify/delete conflict on the three mirrors (the remote commit `9e75f43` had regenerated them) in favour of the deletion; its pre-existing unpushed evidence commit `19979c9` was preserved on top.

Pushes observed, all fast-forward: `iamawriter` `2801662..665276b` (main), `sys-prompts-cc` `30c60ae..0c9bade` (main), `substack-scraper` `33b6953..477061b` (main), `pokemine` `b70ba59..df196c8` (main), `vaultwise` `55b7509..65f56ae` (master), `skills/rubix-review` `9e75f43..16cc36b` (main).

Final verification across all nine repos: no `ISSUES.md` / `BACKLOG.md` / `WAYFINDER.md` / `docs/chain-state.md` remains on disk; `config/repo-state.md` and `config/conventions.md` are kept in every repo per the doc, left for manual removal; each of the six recovered or rebased repos re-fetched and reported `0	0` against upstream.
`pokemine`'s seven unrelated modified files stayed uncommitted throughout, because every commit named only the mirror pathspecs.

With Sections 2 (above), 4, and 5 now recorded, C3 on RIT-UADV2213 is complete.
Two follow-ups are left, neither part of C3: the now-orphaned `scripts/gen-mirrors.sh` in each rolled repo, and the `config/repo-state.md` / `config/conventions.md` files left for manual removal when the loop layout is fully retired per repo.
