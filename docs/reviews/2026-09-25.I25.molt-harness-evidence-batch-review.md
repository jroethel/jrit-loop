# Batch review: molt harness-evidence loop run

Gate journal for the /loop-drive run of `docs/plans/2026-09-25.loop-molt-harness-evidence-plan.md`.
Created when autonomy took effect (session override `auto`, set 2026-09-25); appended at every gate in chronological order.
ASK and STOP entries are record-only; BATCH and DEFAULT entries are the review obligation at the end-of-chain checkpoint.

## Entries

### 1. ASK (record-only) - autonomy set to auto

- Decision: the user set the session override to `auto` via /loop-auto; the consolidated pre-autonomy ASK was empty (no offers or choices pending).
- Rationale: user command; repo default stays `pause` in `docs/loop/pointer.md`.
- Reversal path: n/a - resolved live.

### 2. BATCH - Step 0 topology lean

- Decision: one topology, following the source plan's own dependency graph: wave 1 = T1/T2/T3 parallel, wave 2 = T4-T9 serial (D1-D6), wave 3 = T10, wave 4 = T11 + T12 parallel, wave 5 = T13 (T12 may still be running), wave 6 = T14.
  Whether the D1-D6 chain packs as one worker or six sequential workers is left to the drive-compile dispatch.
- Rationale: the plan spells out its dependency graph explicitly; no second shape came within 60/40 of it.
- Reversal path: taste reversal - scoped re-run of the compile with an alternate wave grouping.

### 3. BATCH - run ticket filed at pre-flight, not now

- Decision: the run has no logged tracker item, so the journal is created without token segments; one run ticket is filed at the pre-flight step and its `.I<n>` token inserted into this filename then.
  Task 1's unit additionally references existing issue 24, whose subject it resolves.
- Rationale: the journal grammar explicitly allows creating without tokens and inserting on item creation; deferring the outward tracker write to pre-flight keeps Step 0 read-only.
- Reversal path: cheap - close the ticket and rename the journal.

### 4. ASK (record-only) - session override re-set to auto on resume

- Decision: the prior session died between Step 0 and the drive-compile dispatch; the user resumed with /loop-auto `set auto`, and the consolidated pre-autonomy ASK was again empty (no offers or choices pending in this session).
- Rationale: a session override lives in conversation only, so the new session re-sets it; the repo default stays `pause`.
- Reversal path: n/a - resolved live.

### 5. DEFAULT - resume point: relaunch at the drive-compile dispatch

- Decision: resume by relaunching the drive-compile dispatch (Steps 1-4 and 6) fresh, keeping entries 1-3's Step 0 decisions (route: multi-wave dependent build; topology lean; run ticket deferred to pre-flight).
  Evidence: no `_loop.md` exists next to the source plan, no run ticket is filed, and the journal ends at entry 3, so the prior session died before compilation emitted anything.
  Probes re-run this session: ringer present at `~/repos/ringer`; herdr present and `herdr api schema --json` succeeds.
- Rationale: the reconciliation rule relaunches (never resumes) a half-done unit, and compilation left no partial artifact to reconcile; Step 0's journaled decisions are decisions, not stale state, so they stand.
- Reversal path: cheap - discard the compiled `_loop.md` and re-run Step 0 from scratch.

### 6. BATCH - compile decisions accepted at the pin review

- Decision: accept the compiled orchestration plan `docs/plans/2026-09-25.loop-molt-harness-evidence-plan_loop.md` as emitted: all implementers on the ringer transport, background agents for validators only; D1-D6 as six one-task serial ringer waves on glm-5.3 (claude-zai lane); U11F (Task 11's fixture) moved forward to wave 1; verbatim executor writes handled by haiku clerk tasks; every deviation marked in its section 4.
- Rationale: pin review passed - no em dash, tables aligned, one sentence per line; the `--baseline` flag the dry run uses exists in `ringer.py run --help`; the glm-5.3 docs posterior cited in the routing table (proven, 10 tasks, 80% first try, 80% pass, 1 amended) matches the live `./ringer.py models --task-type docs` output; human checkpoints H1-H6 all survive in the 6.4 ask-the-human table.
- Reversal path: taste reversal - scoped re-run of the compile with the alternate lean (single sonnet worker for D1-D6, or worker-side pointer writing).

### 7. BATCH - pin-review rulings on the compile's seven open items

- Decision: (1) reviewer conduct contract pasted into validator prompts and the U12C scorer only, not implementer specs or fresh-session briefs; (2) source-plan ruling 10 wins over the worker-written resume pointer for U10/U11/U13 - the orchestrator writes those pointers; (3) the Task 12 "nothing kept" outcome (C4 fails by construction) defers to H3, where the owner rules; (4) one-sentence-per-line applies to added or rewritten lines only, untouched lines are not reflowed; (5) the exact-path ledger predicates stand as compiled, and a miss is an owner ruling, never a ledger rewrite; (6) U12B (the kanban arm runs) runs as an orchestrator background shell, a deliberate exception to the ringer model-calling rule; (7) the run ticket files inside pre-flight item 1 so the journal filename and commit grammar get its number.
- Rationale: (1) and (2) are forced by ruling 10's fixed brief input; (3) is conditional and owner-owned; (4) is the conservative surgical reading; (6) the script is pre-registered and judgment-free, a model intermediary adds a blinding surface, and H6 gates the spend anyway.
- Reversal path: (6) is the only lean with a live alternative - swap in the compiled fallback (a haiku worker launching the same script detached) before W4b; the rest reverse with a scoped recompile.

### 8. DEFAULT - execution transition and launch

- Decision: with the knob at auto, the pin-review pass is the execution approval; the pre-launch detail menu's nothing-selected default (launch immediately, dashboard and watch points printed inline) is auto-taken; run ticket #25 filed, journal renamed to carry `.I25`.
- Rationale: loop-drive Step 6/7 under auto; invoking /loop-drive on the finished plan under auto is the execution approval.
- Reversal path: cheap - stop the run at the next gate; every gate through H5 is reversible by `git revert` on the integration branch.

### 9. Record - pre-flight executed clean

- Decision: pre-flight items 1 to 11 all pass: run ticket #25 filed and claimed (session fable5-molt-20260925), issue 24 labeled and claimed; integration branch `loop/molt-harness-evidence` at BASE f7e61b3c43765e7b962ddee8b8ac5164c82db390; claude 2.1.280 with catalog tag v2.1.280 (2026-09-22); qmd present; `~/.config/jrit` clean at d82bf9a; `~/repos/kanban` clean, no docs/; ringer MODEL-NOTES clean; all seven baseline scripts pass and consumer-sweep fails as the plan premises; engines claude and claude-zai configured; Ringside already serving on 8700; extraction sanity clean; baseline dry run: 4 of 4 checks fail at the resume-pointer step, none at scope; no owned path gitignored.
- Rationale: every observation matches the compile-time record in the _loop.md; no STOP fired.
- Reversal path: n/a - record only.
- Custody manifest (checks.sha256, 42 entries):

      adee1480396bf96c47c664e34db144ea77ce2696022fbfe2253f5492428d4b35  checks/u01-sweep.plan.sh
      2b30d08ae899181a2d107e2edf3b13a898b49392012a261f61339c14d60c334a  checks/u01-sweep.sh
      70103ae5be1d5676e50d4073942a33c74e05671fdedf03a06c3a79375556de6f  checks/u02-molt-home.plan.sh
      66d52fc033f5924e3259ba6962f2b6faf6bd2f1d584aca36d6769bdb1d057e85  checks/u02-molt-home.pre.sh
      6e7a8686ef8979a29bc2ea2fdc5a8e619165069eb172c6eaf964256c7c25ecd8  checks/u02-molt-home.sh
      d780082631eea8acb9364d79b45be9e5b4d3c088c7b1f14d2a3af408fa1de681  checks/u03-evidence.plan.sh
      7b210814775585c1fa613efc2ef37197e091ef6ffec3d1169eeb2ab26d2d2194  checks/u03-evidence.sh
      0169667e52853149cc1c436d0a66b559950d6a437a8e97a7233d614181e7a802  checks/u04-d1.plan.sh
      af152ce9b8a9b4dae537cc87e623470ba8624b686d00893fdc29e747dee8ebc1  checks/u04-d1.sh
      1fafcfd892b705258696e0ddd0324ed0c790f70af6777d302391b7a0eed9e4d2  checks/u05-d2.plan.sh
      bb9581bcdaca7ee5b97bc9b8befa7db00d34f5fbe23340a258beb69529d98aa2  checks/u05-d2.sh
      26087906412ea7890f642f8ef7e0f8abdf479d17d14e956e25dfb43e6e4cac64  checks/u06-d3.plan.sh
      cafcb178a4b26438fa5c5d3c6a81c24ef775284c4986fc7dd3ffc4ce515e014f  checks/u06-d3.sh
      965780a4704e8527134dbd6f3912a62a42e00dfd63730e4555e7f92362ed84a8  checks/u07-d4.plan.sh
      6a86cef1e4ed4ff2d5e9e5c534b9e090efb4efce2595cd0cd1bebe4145116738  checks/u07-d4.sh
      b80dea40873b2b3c77c518b1a11b7635a37bf17288d05228b30fcb1b574af938  checks/u08-d5.plan.sh
      46e8d07efaacab93fea3f29520678c77395b0c317050b94e0a373480bb62bb1b  checks/u08-d5.sh
      db68df1924d4a48d91fb4aa869fd44c5c466ea058d785ca449f405883c96417e  checks/u09-d6.plan.sh
      6ed4fea80c8340725ef500e5c60cc34f606d90a333557ad1e524082567980a8d  checks/u09-d6.sh
      c9d01133687a177660c67a9b431b45b9ebc29887a64287397ef532bf6cc86184  checks/u10-molt-on-molt.plan.sh
      0850ed6213d3c59a0944d995204aeafd3d364f4196a159c415d4e1eeb22c097b  checks/u10-molt-on-molt.sh
      0f9ace4b6cf312b0cca18c53469441c25399f0aa0dd5715532313160f89eae32  checks/u11-fixture-run.plan.sh
      cd78bbb1352d2539a76c82270d95daea087aae0352bbc18de46942e164fe6710  checks/u11-fixture-run.sh
      6e00adc9103312ec059fec4ddf99badfa15ea0ae8e82c94ee89417c0641c40f5  checks/u11f-fixture.plan.sh
      be116f52883c5833335edee0e6d6431983f475e53d4ccf1cdc3be01bb8933fec  checks/u11f-fixture.sh
      c97930006c5d949a6aed1258be5cbebaf5b240e6c2dcca8c63f5098ac1fd3551  checks/u12-c4.plan.sh
      d92e1c56874349f780c3fa02ca9e5fa7c16fb0d0562fcef4b820ab921a80037e  checks/u12e-kanban-ledger.pre.sh
      08afc309a42f7bfacf5d938663f1ad59e5bb51d4078a1de9491c2a1742f10f91  checks/u13-instr-files.plan.sh
      0eeb7c94cdd9f263d00d515bd41a5d9ebc74e3e890211caf5f16b7c28440085a  checks/u13-instr-files.pre.sh
      393b2c578eb17b42af3b290b9650a7ef45e680cef8dae844a1c399a55f44cfc5  checks/u13-instr-files.sh
      7d2594e743de97be0577d7c18e0590997bc97b792892d2840b94f44a0a99cb1a  checks/u14-final.plan.sh
      0e96460de87cd3cf447bb7c293d140dffcf5a7e5df530ed614965183bda4ec20  expected/evidence.md
      45689f771233472e716cdcdfde6b03e83568e3f7d1e48b8d258dabbd9cfa5c26  expected/fallback-ledger-header.md
      1376f1f451c28f8a3d78ec6d935370035eb6ea5dd62fd26f487167609dd44066  expected/fixture.md
      a8245b11b1902e24323506f97b694c7c9f7c1ec9cfa8952f1b8532942eaba021  expected/gitignore-tail.txt
      8bc8585ee56309e84dd964712d52806faf640a70b04bb0200e37d3899d40bf36  expected/kanban-ledger-header.md
      a6b2a1dc718a57de9578cef18b45682c18dc07f4c7a78d256be296dffb1a8c86  expected/keep-rule.txt
      ba18801b598f000f3422d9750fb417b4e0aeffdb79fc2b9051a176b84431e73e  expected/ledger-template.txt
      c2c2eabef1e947a77ebaa2e9a3c7252a1fef9fd38064b8cf6fa446450bef8393  expected/stimulus.txt
      71f75a2593bdec6be09fdef55a3a02871259404548cde40bc385abe9a7f8b551  expected/sweep-comment.txt
      574c9ad56c3834d4b1db6f89f9526d7a74edc31c21c140c452a5be12f996d7f2  expected/u02-download.sh
      fc00941a5ea0fa4e12c4a317141e400a20cc9bde0c815d75fdfa554f31a67373  expected/u12b-runs.sh

### 10. BATCH - date-dependent check wrappers authored at their wave, not pre-flight

- Decision: the wrappers for U12A to U12E and the clerk checks depend on the run date and the H1/H2 answers, so they are written at their wave's launch and appended to checks.sha256 then, each append journaled.
- Rationale: their content cannot exist at pre-flight; custody still holds because every wrapper is sha256-pinned before any worker that it judges runs.
- Reversal path: cheap - re-pin and re-run the wave's checks.

### 11. Record - wave 1 gate closed

- Decision: all four W1 units passed on first attempt (run molt-harness-evidence-20260925T064805Z-p3440249); custody intact; patches applied and re-checked fresh on the integration branch (T1-OK, T3-OK, FIX-OK) and committed as e3229d2 (u01), f38b854 (u03), 61d9413 (u11f); the full suite is green including consumer-sweep, resolving the baseline failure; U02's in-repo pointer patch is held for G02.
- Rationale: gate procedure 6.1; run JSON is truth and matched the shell output.
- Reversal path: cheap - git revert the three commits on the integration branch.

### 12. BATCH - issue 24 closed at the W1 gate

- Decision: issue 24 closed through the receipt helper with the executed check `bash ci/consumer-sweep.sh` cited, and a closing comment naming commit e3229d2.
- Rationale: the compiled plan's W1 gate item; U01 is the fix for issue 24's subject.
- Reversal path: cheap - `gh issue reopen 24`.

### 13. ASK (record-only) - H5 resolved

- Decision: the owner approved the `~/.config/jrit` .gitignore commit and ruled nothing un-ignored; G02 then ran Task 2 Step 4 verbatim (config commit d5d0d1d), the full T2 check printed T2-OK, and U02's pointer was committed (14eefa5).
- Rationale: H5 is the owner's gate; fail-closed ignore rules stand.
- Reversal path: n/a - resolved live.

### 14. Record - W2a (D1) gate closed, with one validator-conduct finding

- Decision: u04-d1 passed its ringer run first try; custody intact; patch applied, D1-OK fresh, suite green; the opus validator returned pass on every criterion; D1 committed with the plan's message.
  Finding: the validator ran `tests/receipt-gates.sh`, which the plan bars (outbound GitHub repo create/delete); its cleanup held (repo list shows no strays), and the validator template now bars that script and ci/run-all.sh explicitly.
- Rationale: gate procedure 6.1; the conduct breach traces to the orchestrator's validator prompt not naming the barred scripts, a template fix under the distill step (BATCH, one template, under 15 lines).
- Reversal path: cheap - git revert the D1 commit; the template fix reverses by removing the added line.

### 15. BATCH - D2 validator fail, one repair relaunch

- Decision: the D2 implementer's ringer check passed but the opus validator failed one criterion (the rewritten SKILL.md step 1 line carried three sentences on one physical line; all content criteria passed); the applied patch was reset and the unit relaunched once with the itemized finding appended to its spec (a findings block under 15 lines, changing nothing the unit produces).
- Rationale: the compiled repair path (section 8.6): one relaunch with findings, a second failure stops the unit.
- Reversal path: cheap - the reset already reversed the patch; the spec findings block is removable.

### 16. Record - design issue slipped: the model-notes file-name rule has no restating owner

- Decision: the D2 validator found a contract gap in the source plan: Task 2's Interfaces says the file-name rule (a page saved as its slug with any leading prompting- dropped) is restated in SKILL.md's host defaults by Task 5, but Task 5's contract lists only three host-defaults items and omits the rule; the D2 check does not require it.
- Rationale: slipped under 6.3 as a design issue to the final checkpoint and H4, because adding the restatement would change what U05 produces (a STOP-class spec edit), and nothing downstream in this run consumes the restated rule.
- Reversal path: n/a - recorded for the owner; the one-line addition can ride any later commit if ruled in.

### 17. BATCH - D2 second validation fail; unit re-routed to opus, template distilled

- Decision: the repaired D2 attempt fixed the skill files (every content criterion passed, both plan checks green) but failed one criterion again - the worker-authored resume pointer held two sentences on one physical line; two validation failures stop the unit, and it is re-routed at the gate to opus on the claude engine (max_attempts 1) with both findings in the spec.
  Distill: the shared worktree spec template now states that every Markdown line in the resume pointer holds one full sentence, so D3-D6 do not re-earn this failure.
- Rationale: the runtime re-route rule (6.1); glm-5.3 was content-correct twice and style-undisciplined twice on this unit, so the evidence points at instruction-following discipline, opus's strength; the template fix is confined, under 15 lines, and changes nothing any unit produces.
- Reversal path: taste reversal - relaunch on the claude-zai lane with the sharpened spec if the opus route is judged too expensive next time.

### 18. Record - W2b (D2) gate closed on the opus re-route

- Decision: the opus attempt passed the ringer check in 66 s and the fresh validator passed every criterion (D2-OK and D1-OK re-run, style clean including the resume pointer); D2 committed with the plan's message.
- Rationale: gate procedure 6.1; third attempt, first under the re-route pin.
- Reversal path: cheap - git revert the D2 commit.

### 19. ASK (record-only) - owner ruling: style rules rescoped to human-read artifacts

- Decision: the owner ruled live that the one-sentence-per-line rule was written for output he reads and must not be enforced on machine-read run artifacts; for the rest of this run, resume pointers and worker result files are exempt, and everything else (skill files, README, reports, ledgers, this journal) keeps the rule.
  Applied to: the _loop.md 8.2 global-rules block and 8.6 validator criterion, the extracted global-rules spec block, and the worktree spec template; the durable fix is filed as issue 26.
- Rationale: the D2 unit burned two extra worker runs and two extra validator runs on pointer-doc line wrapping with zero effect on the deliverable; a session ruling from the owner outranks the plan's blanket wording.
- Reversal path: n/a - resolved live; the durable fix lands through issue 26.

### 20. Record - W2c (D3) gate closed

- Decision: u06-d3 passed ringer first try and the opus validator passed every criterion (D3/D2/D1 checks all green, suite green); committed with the plan's message.
  Slipped observation for the final review: the plan-mandated step line `3b. **Gap scan**` is not a valid CommonMark ordered-list marker, so SKILL.md renders it merged into step 3's paragraph; the implementer met the contract exactly, so any fix is a plan-format ruling, not a unit defect.
- Rationale: gate procedure 6.1; first D-unit judged under the issue-26 rescoped style criteria.
- Reversal path: cheap - git revert the D3 commit.

### 21. Record - W2d (D4) gate closed

- Decision: u07-d4 passed ringer first try and the opus validator passed every criterion (D4/D3/D2/D1 checks all green, suite green); committed with the plan's message.
- Rationale: gate procedure 6.1.
- Reversal path: cheap - git revert the D4 commit.

### 22. Record - W2e (D5) gate closed

- Decision: u08-d5 passed ringer first try and the opus validator passed every criterion (all five D checks green, suite green, bins table byte-untouched); committed with the plan's message.
- Rationale: gate procedure 6.1.
- Reversal path: cheap - git revert the D5 commit.

### 23. Record - W2f (D6) gate closed; wave 2 complete

- Decision: u09-d6 passed ringer first try and the opus validator passed every criterion (all six D checks green against the final tip, ledger template byte-identical to the plan's, README row exact, suite green); committed with the plan's message; the revised protocol D1-D6 is now fully landed.
  Validator note recorded: the plan says the SKILL.md Workflow section has two ledger mentions, but HEAD had only one; both real mentions were redirected, so the unit is unaffected.
- Rationale: gate procedure 6.1, including the after-U09 all-six-checks sweep from 6.2.
- Reversal path: cheap - git revert the D6 commit.

### 24. ASK (in progress) - H1 for Task 10

- Decision: partially recorded. Target models (owner, verbatim): claude-fable-5-1; claude-opus-5-5; claude-sonnet-5; glm-5.3 - "setup to use multiple models, but only judge using a single case - fable or opus"; the judging case for gap scan and choreography verdicts is claude-opus-5-5 (owner, second ask).
  The constraint-register confirmation is still open: the owner asked to see the register text verbatim in-session before confirming; being presented now.
- Rationale: H1 is ASK-class; answers recorded as given, nothing assumed.
- Reversal path: n/a - resolved live when complete.

### 25. ASK (record-only) - H1 for Task 10 resolved

- Decision: the owner confirmed, with the register text quoted in-session, that all nine inherited constraints stand unchanged (items 1-7 unflagged across the grouped ask; item 8 "nevermind, leave as is"; item 9 stands after seeing the do-not-worsen text).
  Target models, verbatim: claude-fable-5-1; claude-opus-5-5; claude-sonnet-5; glm-5.3 - "setup to use multiple models, but only judge using a single case"; the judging case is claude-opus-5-5.
  Run date for Task 10 set to 2026-09-25.
- Rationale: H1 is ASK-class; the owner asked for the sources and then the verbatim text before confirming, and both were provided.
- Reversal path: n/a - resolved live.

### 26. Record - K10 gate closed; U10 launched

- Decision: the K10 clerk passed first try (ledger header byte-identical to the plan template, claude-sonnet-5 notes downloaded, custody manifest extended with the K10 check before launch); U10 launched as the blinded fresh session with the plan's brief verbatim, fields filled from H1, max_attempts 1.
- Rationale: gate procedure 6.1 plus the ruling-10 blindness mitigations (no plan content in the brief, no retry, contamination grep at the gate).
- Reversal path: K10 is cheap (delete the header and notes file); U10's append-only ledger makes a failed run a STOP, never an auto-relaunch.

### 27. ASK (record-only) - U10 check-bug ruling and the first H2 answers

- Decision: the owner ruled the D8 FAIL a check bug (the absolute-path Findings-report predicate; the compile's open ruling 5 shape): the check now accepts the ~/ form, D8-OK fresh, custody manifest re-pinned; the audited work stands and the ledger is untouched.
  H2 answers so far, verbatim: H2-2 = one-real-task substitute allowed; H2-3 = "mostly #1.. 'major version release' is how I'd phrase it, but that can be cross-provider as well so it doesn't point to a time cadence."; H2-1 pending (owner inspecting SKILL.md line 21); H2-4 and H2-5 pending.
- Rationale: the gate's attribution rule - re-run the check yourself, and when the worker was right and the check was wrong, fix the check and keep the work.
- Reversal path: n/a - resolved live; the check amendment is one line, revertible.

### 28. ASK (record-only) - H2 for Task 10 fully resolved

- Decision: all five owner questions answered. (1) vendored: original, no upstream - the SKILL.md:21 wording is stale and the rewrite finding stands; (2) executable checks: the one-real-task comparison is an allowed substitute; (3) cadence, verbatim: "mostly #1.. 'major version release' is how I'd phrase it, but that can be cross-provider as well so it doesn't point to a time cadence."; (4) wording standard: both sources, and the best-practices page carries its own fetch-date and sha256 stamp; (5) unattended runs: record and continue.
  KH2 clerk launched to transcribe the answers under their questions, judged against a before-copy diff.
- Rationale: H2 is one question set per run, molt never picks a winner; answers recorded as given.
- Reversal path: n/a - resolved live.

### 29. Record - W3 gate closed (molt on molt landed)

- Decision: KH2 passed (five answer lines, diff-clean); the U10 validator passed all eight criteria, including citation spot-checks against the v2.1.280 tag and the opus sha256; gate closed with the t10 resume pointer committed.
  Validator notes accepted and acted on: (a) blindness - the worker's find listed plan file NAMES but read no plan content; ruled acceptable since no content leaked, and the U11/U13 gate greps will also sweep the session transcript directory, which the worker.log does not cover; (b) the D8 check's config-repo status predicate is vacuous (molt/ is untracked as intended); noted, harmless; (c) a likely missed finding - the v2.1.280 corpus documents claude plugin eval as a native mechanism for molt's reverse tests - slipped to H4 and the final review.
- Rationale: gate procedure 6.1 plus the ruling-10 blindness mitigations.
- Reversal path: cheap - git revert the pointer commit; the out-of-repo artifacts are append-only by design.

### 30. ASK (record-only) - H1 for Task 12 resolved; wave 4 launched staggered

- Decision: the owner ruled the kanban prompt has no deliberate constraints - everything in the prompt text is fair game for proposed additions; target model claude-fable-5-1 stands from ruling 4.
  U11 launched immediately (its register was pre-answered by planning decision 18) and U12A launched after this H1, as two one-task manifests instead of the compiled single two-task manifest; both meet the same gate.
- Rationale: H1 is ASK-class; the stagger is a packing detail that changes no product and let U11 start during the ask (BATCH).
- Reversal path: n/a for the ruling; the stagger has no state to reverse.

### 31. ASK (record-only) - T11 H2 and T13 H1 resolved; U11/U12A checks green

- Decision: T11's single H2 answer: the fixture's B6 reason meant jrit-loop's receipt.sh and the claim is false (the audit's R1 finding confirmed); transcribed by the kh2-t11 clerk, diff-clean (KH2T11-OK).
  T13 H1: nothing protected (the audit may question any rule in the three files); readers claude-fable-5-1; claude-opus-5-5; claude-sonnet-5; glm-5.3 with judging case claude-fable-5-1; self-manual.md confirmed pipeline-generated.
  U11 passed first try (C2-OK: restated block flagged with harness file, version, index date; decoy untouched); U12A passed (U12A-OK; three additions pre-registered: Formatting in chat, Writing density, Quoting retrieved sources); both contamination sweeps clean (U12A's one hit was the learning-guide skill description in the worker's own system prompt, not a kanban read).
- Rationale: H1/H2 are ASK-class, recorded verbatim; gate procedure 6.1.
- Reversal path: n/a - resolved live.

### 32. Record - U11 gate closed (criterion 2 proven)

- Decision: the U11 validator passed every criterion - all four planted restatements verified line by line against v2.1.280, the decoy kept as policy, ledger entry complete, style clean; gate closed with the t11 resume pointer committed; wave 5 opens.
- Rationale: gate procedure 6.1; brief criterion 2 is now proven by C2-OK plus the validator walk.
- Reversal path: cheap - git revert the pointer commit.

### 33. BATCH - U12A validator fail, one repair relaunch before the checklist freezes

- Decision: the U12A validator passed ten criteria but failed the experiment design on one: the b3 (Quoting retrieved sources) checklist items are not blind-scoreable (I9 depends on state the scorer never sees, I10 and I11 cannot differ between arms); one repair pass relaunched with the itemized findings, letting the implementer either rewrite b3's items observable-in-text or drop b3 with a stated reason; the report also gains the plan-required user-level-instructions confound line and two labeling corrections.
  Timing matters: the checklist freezes at the first arm run, so this is the last moment the fix is possible; H6 waits for the repaired design.
- Rationale: repair path 8.6 - one relaunch with findings; the design choice (rewrite vs drop) belongs to the experiment designer, not the orchestrator.
- Reversal path: cheap - the attempt-1 spec is preserved at the workdir; nothing is frozen yet.

### 34. Record - U12A gate closed after one repair

- Decision: the repaired design passed revalidation on every criterion: b3 dropped with a stated not-observable reason, b1 and b2 byte-unchanged (mtimes predate the repair), the confound line present, checklist sha256 re-pinned and byte-equal in blind/; two additions go to the reverse test (b1 Formatting in chat, b2 Writing density).
- Rationale: gate procedure 6.1; the repair landed before any run, so nothing frozen was touched.
- Reversal path: the attempt-1 spec and report are superseded; a design re-run remains possible until the first arm run fires.

### 35. BATCH - U13 check-bug fix under the U10 precedent

- Decision: U13's FAIL attributed to the check, not the audit: the report's C2 finding names exactly the plan-required CLAUDE.md conflict but quotes the Surgical Changes section's body lines rather than its heading phrase, and the check grepped only the literal 'Touch only what you must'. The predicate now accepts any verbatim Surgical-Changes line; C1-OK fresh; the audited work and its already-appended ledger entry stand untouched.
- Rationale: the owner's U10 ruling established the class (over-literal check predicate vs a substantively correct fresh session, fix the check and keep the work); a second identical class is applied as precedent rather than re-asked.
- Reversal path: cheap - the predicate edit is one line, revertible; the owner can reverse at the end-of-chain review.

### 36. ASK (record-only) - Task 12 H2 and H6 resolved

- Decision: H2-1: line 7 only requires write-at-work-time, so line 14's premise test stays legitimate; H2-2: the premium-model clause on line 10 came from the retired premium-access constraint, and cost plus cross-vendor portability remain.
  H6, verbatim: "for this test cap effort at medium." - nine runs approved (3 arm A, 3 each for b1 and b2) at effort medium, overriding the plan Step 11 script's --effort high for this test; the cap is journaled here and recorded in the report before any run.
- Rationale: ASK-class gates, owner answers recorded as given; the effort override is the owner's budget authority at H6.
- Reversal path: n/a - resolved live.

### 37. ASK (record-only) - H2 for Task 13 resolved; U13 validator pass; U12B transport correction

- Decision: the four T13 owner answers: (1) unrelated defects are fixed in the same change; (2) learning-guide, README, and handoff-doc updates are a standing part of every request, made unasked; (3) unclear requests proceed under a stated assumption, asking only when a wrong guess is unsafe or wasteful; (4) an explicit yes on a named irreversible step executes - the model runs it and reports.
  The U13 validator passed all six criteria (blindness clean at transcript level, fixtures read-only and byte-equal, report shape and substance verified, ledger complete).
  U12B transport correction (BATCH): the first launch went through the harness Bash tool, whose 10-minute cap would kill a 15-45 minute run mid-flight; it was stopped at one in-flight run with zero consumed outputs, the mapping and partial output were deleted, and the runner relaunched detached via setsid outside the harness timeout; the mapping regenerated before anything read it, so the experiment's blinding and pre-registration are unaffected.
- Rationale: H2 answers recorded as given; the transport correction prevents the killed-mid-run STOP that would have forced a full re-spend and an H6 re-ask.
- Reversal path: n/a for the answers; the runner can be killed and re-run whole until scoring begins.

### 38. Record - U13 gate closed (criterion 1 proven); H4 held for H3

- Decision: KH2-T13 diff-clean, full C1-OK, gate closed with the t13 pointer committed; H4 stays open by design until the owner has the Task 12 scores table (H3) at the same checkpoint.
- Rationale: gate procedure 6.1 and the plan's H4 wording.
- Reversal path: cheap - git revert the pointer commit.

### 39. ASK (record-only) - U12B out-09 stub, owner-approved single-id re-run

- Decision: eight of nine arm outputs came back healthy (2384-3809 bytes); out-09 was a 228-byte stub because that run wrote its design to a plan file under --permission-mode plan and stopped for approval instead of printing it; the owner approved re-running exactly out-09 with the same mapping row and invocation.
  Blinding preserved: the mapping row is read inside the detached script and never printed; the orchestrator has not read mapping.tsv; the checklist sha256 still matches.
- Rationale: the compiled W4b STOP - the owner decides whether exactly those ids re-run with the same mapping.
- Reversal path: the re-run overwrites only out-09.md; a repeat stub returns to the same STOP.

### 40. ASK (record-only) - H3 resolved: nothing kept, dropped-result ledger ruled in

- Decision: the blind-scored reverse test kept neither addition (arm a mean 5.000; b1 5.000, a tie; b2 4.333); after reviewing the judging summary (~/.config/jrit/molt/runs/2026-09-25.molt-fable5-kanban/judging-summary.md), the owner ruled the dropped-result ledger entry is written and criterion 4 records as a faithful negative result; the plan's C4-OK token stays unmet by construction and is so noted for the final verification.
  Also resolved: the Task 13 generator-routed findings are PARKED, not handed to the distill pipeline.
  U12E relaunched under an orchestrator-authored dropped-outcome check (custody-pinned), since the plan's C4 check demands a kept addition.
- Rationale: H3 is the owner's gate and open ruling 3 assigned him exactly this decision; no checklist rewrite, no keep-rule change, no added runs.
- Reversal path: the ledger file is uncommitted until the outward STOP; delete it to reverse.

### 41. Record - U12E gate: orchestrator check bug, work correct

- Decision: U12E's two ringer FAILs were caused by the orchestrator-authored check (a grep pattern starting with "- " parsed as an option; missing --); with the one-character-class fix the check prints U12E-OK against the worker's first output; the dropped-result report lines and the kanban ledger are correct as produced.
- Rationale: the gate's attribution rule; the check bug is the orchestrator's, not the plan's and not the worker's.
- Reversal path: n/a - check fixed and custody re-pinned.

### 42. ASK (record-only) - outward kanban commit approved and made

- Decision: the owner approved the one-file outward commit; Task 12 Step 16 ran verbatim; the Task 12 chain is closed with the t12 resume pointer committed.
- Rationale: the outward-facing STOP; blast radius one local commit, no push.
- Reversal path: git -C ~/repos/kanban revert --no-edit HEAD.

### 43. Record - W6 gate closed: FINAL-OK, independently re-derived

- Decision: the orchestrator's FINAL-OK was re-run by an independent opus validator, which also re-derived the reverse-test means by hand and walked all five checkable success criteria; run ticket #25 closed through the receipt helper with the executed check cited.
- Rationale: gate procedure 6.1 and the Task 14 contract.
- Reversal path: cheap - git revert the pointer commit; reopen the ticket.

### 44. ASK (record-only) - end-of-chain checkpoint resolved

- Decision: the terminal loop-review ran both axes (Spec: 4 findings, worst H4-unrecorded; Standards: 4 hard style lines in orchestrator prose, rest judgment calls). The owner then: confirmed the U13 check-loosening precedent (entry 35 stands); selected three fixes - the plan's D8 and C1 checks amended in the plan file (aa6dd87), the D5 wording bounded plus the file-name rule added via a worker relaunch (62b0b96), the four .before-h2 copies deleted; declined the journal-prose style reflow; HELD the merge to main.
  H4 completed: after confirming that acceptance changes none of his files, the owner marked all 19 findings accepted; KH4 appended "Accepted 19 of 19" (KH4-OK, nothing above the section changed); criterion 6 is recorded.
- Rationale: the final checkpoint is the owner's; every auto-taken decision in this journal was presented with the review.
- Reversal path: the merge is unheld by one command; the two fix commits revert cleanly.

### 45. ASK (record-only) - owner-directed follow-up: setup and maintenance documented

- Decision: the owner asked where an operator learns to acquire the Piebald catalog and maintain the qmd index, found it documented nowhere, and directed the fix now; a worker added three Host defaults lines (one-time clone with the fail-soft consequence, post-upgrade fetch --tags, owner-run qmd update --pull plus qmd embed -c piebald-ai marked never-during-an-audit); all six D checks and the suite stay green.
- Rationale: owner instruction resolved live; the model-notes path already had this treatment and the corpus/qmd paths now match it.
- Reversal path: cheap - git revert the commit.

### 46. ASK (record-only) - owner fired the push and merge

- Decision: on the owner's explicit instruction, loop/molt-harness-evidence was pushed to origin and fast-forward merged into main; main pushed after this entry.
- Rationale: the held merge from the end-of-chain checkpoint, released by the owner.
- Reversal path: git revert on main, or reset before anyone builds on it; the branch ref remains at origin.
