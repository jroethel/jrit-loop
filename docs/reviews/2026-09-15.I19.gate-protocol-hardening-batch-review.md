# Gate journal: gate-protocol hardening (issue #19)

Run date: 2026-09-15.
Chain: /loop-auto set auto -> /loop-plan on `docs/briefs/2026-09-15.I19.gate-protocol-hardening-brief.md`.
Mode: auto (session override; repo default is pause, no `docs/loop/pointer.md` in this source repo).
Autonomy took effect after the Step 2 ASK gate below resolved.

This journal is created the moment autonomy takes effect and appended at every gate in chronological order.
ASK and STOP entries are record-only (the human was present).
BATCH and DEFAULT entries are the review obligation: accept or reverse at the end-of-chain checkpoint.

## Entries

### 1. set auto - consolidated outstanding ASK (record-only)

- Decision: on `set auto`, presented the consolidated "outstanding before autonomy commences" check. Nothing was outstanding: the user's "yes rubix / accept recommendations" pre-answered Step 6, and no other offer or route choice was on the table. Proceeded to the Step 2 gate.
- Rationale: a mode change never retroactively answers a pending question; there were none pending, so the set was clean.
- Reversal path: n/a - resolved live.

### 2. loop-plan Step 2 - resolve open questions `[gate:ASK]` (record-only)

- Decision: asked the brief's two open questions live before autonomy took effect. Q1 (M2 fixture-convention home): the user chose the ringer Check-writing ruleset as the single governing home, with loop-drive carrying a one-line pointer; the full ringer-side statement is a separate cross-repo follow-up, not this plan. Q2 (M1 journal-commit grammar): the user chose `Journal #N: <imperative>`, dropping `#N` when the item is unlogged.
- Rationale: both are genuine design/scope decisions, not facts; Step 2 ASK blocks under autonomy and these were the last ASK gate before autonomy commenced.
- Reversal path: n/a - resolved live.

### 3. loop-plan Step 6 - Rubix review (DEFAULT) and triage (BATCH)

- Decision (DEFAULT, ran the review): ran the two-lens Rubix review on the plan, honoring the user's pre-approval "yes rubix". Lens A (impacted professional, Opus) took the next-orchestrator seat; Lens B (cold craft, Sonnet) read against best practice. Both fresh-context and conduct-bound.
- Decision (BATCH, triage): accepted the reviewers' recommendations per the user's "accept recommendations". Folded in every substantive finding: wired M1's journal-commit into loop-drive's executable pre-flight checklist (was prose-only, so the emitted plan would have omitted it while the grep still passed); added M2's "on demand only, never a per-run obligation" clause plus an interim fallback so a run is never trapped before the ringer convention lands; dropped the vacuous M1 same-file ordering grep and reclassified criterion 3's cross-file reading order to the human checkpoint with a scripted proxy; unified the M1 cross-file phrasing on "pre-flight journal-commit step"; added M4's deliverable-versus-spec boundary so it stops colliding with the BATCH 15-line spec-edit and fix-the-check allowances; dropped the trivial negative greps and rewrote the plan's verification claims honestly; corrected the dependency graph so Task 4 runs parallel to the Task 2 to Task 3 chain; fixed the Global Constraints mischaracterization of the newly introduced Journal #N grammar. Lens B's grep-to-wording coupling nit was noted in the plan, no change.
- Rationale: the two lenses converged independently on the two highest findings (M1 pre-flight wiring, M2 on-demand qualifier), and every folded finding either restores fidelity to a settled brief ruling or closes a check that passed while its invariant rotted. Re-ran Step 5 on test-applied copies of both skill files: all four acceptance checks print their -OK token and zero em-dashes are introduced.
- Reversal path (BATCH taste reversal): the revision is a scoped re-edit of the plan document; revert by restoring the pre-triage draft from git history or re-running Step 6 with findings dismissed. Cheap, since the plan is a docs artifact not yet applied to the skills.

### 4. loop-plan Step 7 - user review and commit offer (DEFAULT)

- Decision: auto-took the commit of the plan and this gate journal (docs/plans and docs/reviews), which the DEFAULT gate declares under auto. Did NOT push, since a push is outward-facing and would be a STOP. The two skill files are untouched: applying the four edits is the downstream executor's job, not loop-plan's.
- Rationale: under the auto knob the Step 7 commit offer auto-takes; the artifacts are the intended tracked loop outputs and git status carried no stray files.
- Reversal path: cheap - `git revert <sha>` or reset the local commit; nothing was pushed.

### 5. loop-plan Step 8 - hand off (DEFAULT)

- Decision: closed by naming the approved plan path and the onward route (/loop-drive front-door triage, or execute the four edits by hand). loop-plan does not invoke the next skill; the route is the human's.
- Rationale: hand-off is the terminal DEFAULT; no judgment call remained.
- Reversal path: n/a - informational.

## Entries: loop-drive phase (same chain, new session, 2026-09-15)

Chain continued: /loop-auto set auto -> /loop-drive on `docs/plans/2026-09-15.I19.gate-protocol-hardening-plan.md`.
Mode: auto (session override; repo default still pause, `docs/loop/pointer.md` still absent).

### 6. set auto - consolidated outstanding ASK (record-only)

- Decision: on `set auto` in the drive session, nothing was outstanding; no offers or route choices were pending at session start, so autonomy commenced clean.
- Rationale: fresh session; a mode change never retroactively answers a pending question, and none were pending.
- Reversal path: n/a - resolved live.

### 7. loop-drive Step 0 - route is ONE AGENT (BATCH)

- Decision: One-Minute Test verdict ONE AGENT; the whole plan runs as a single unit (all four tasks in dependency order 1, 2, 3, 4) with one implementer and one validator, no wave machinery.
- Rationale: four fully-specified verbatim edits, serialized by shared-file ownership except Task 4; the only available parallelism (Task 4 beside Tasks 2-3) is minutes of work and not worth a second worker or a sync gate.
  Alternate shape considered: a ringer 3-run split (T1; then T2 plus T4; then T3) with per-run gates; rejected as pure gate overhead for the same edits.
- Reversal path (BATCH taste reversal): scoped re-run of the plan with the alternate split; cheap, since every task is idempotent against its anchors and greps.

### 8. loop-drive Step 2 - transport background-agent, model haiku-4.5 (BATCH)

- Decision: the unit takes the background-agent transport with haiku-4.5 as implementer (task_type docs, impl effort medium) and the background-agent-validator role pin (opus, effort medium) as validator.
- Rationale: the routing chain's top evidence tier pointed at glm-5.2/docs (posterior: 35 tasks, 86% first try, 91% pass, proven; MODEL-NOTES integrity read clean, the pending stm-nav amendments would only raise it), but glm runs only via ringer, and ringer's lint hard-blocks worker `git commit` under worktrees (repo substrate note dated 2026-09-15, `ringer.py:1830`), while this plan's acceptance includes four exact per-task commit messages that must survive to the gate.
  The background-agent transport preserves the worker's branch and commits; on that roster no model has trustworthy docs scoreboard rows (opus/docs is 1 row, probation), so the choice falls to the benchmark prior, and haiku-4.5's row ("thin, mechanical, well-referenced units") matches verbatim insertions at named anchors with given grep checks.
  Evidence tags: transport pin:commit-survival; model prior.
- Reversal path (BATCH taste reversal): relaunch the unit via the ringer split with orchestrator-authored commits (glm-5.2), or re-route to sonnet-5 on a validation double-fail per the runtime escalation rule.

### 9. loop-drive Step 6/7 - execution transition and launch (DEFAULT)

- Decision: invoking /loop-drive on the finished I19 plan with the knob at auto is the execution approval; the pre-launch detail menu is not fired, the nothing-selected default (launch immediately) is taken, and the dashboard, dry-run results, and watch points print inline.
- Rationale: the loop-drive Step 6 and Step 7 rules for knob-at-auto.
- Reversal path: cheap - the worker can be stopped mid-run and its worktree pruned; merged commits revert with `git revert`; nothing is pushed.

### 10. Journal overwrite incident, self-caught and repaired (record)

- Decision: the drive session's first journal write replaced this file's loop-plan entries 1-5; the commit diffstat exposed the loss, the prior content was restored from `HEAD~1`, and the drive entries were appended after it in the same amended journal commit.
- Rationale: the journal is one appended file per chain, not one per session; the overwrite was an orchestrator error, caught before any further gate fired.
- Reversal path: n/a - repaired in place; the transient bad version exists only in this commit's amend history.

### 11. Wave gate - validation pass and merge (DEFAULT)

- Decision: the haiku-4.5 implementer returned all four tasks green (pass attempt 1, 0 repairs, 136s); the opus validator passed all seven criteria on executed evidence (18/18 acceptance predicates rerun, programmatic verbatim compare of every inserted block, in-place anchor audit, scope and commit-structure audit, unicode em-dash scan sanity-tested against a known U+2014 byte); the unit branch was rebased onto main and fast-forward merged, and all four acceptance one-liners were re-run green on merged main.
- Rationale: standard gate procedure under auto; the rebase exists because the orchestrator amended the journal commit to strip a co-author trailer that violates the user's global commit-message rule, which had shifted the branch's parent sha.
- Reversal path: cheap - `git revert` the four merged commits (7254c38, 790406e, 67f53e7, 30ea047); nothing is pushed.

### Noted deviations (once, drive phase)

- `scripts/receipt.sh` is inoperative here: this repo has no `docs/loop/pointer.md` and therefore no `tracker:` key, so claim enforcement is unavailable; `AGENT STATUS` receipts are posted to issue #19 with `gh issue comment` directly.
- The unit's closing commit adds the protocol-mandated resume pointer (`docs/handoffs/2026-09-15.gate-protocol-hardening.resume.md`) beyond the plan's literal `git add` list for Task 4.
- The final-wave advisory /loop-review is not run: the ONE AGENT route skips the wave loop it belongs to, and the plan's own human checkpoint (criteria 3 and 4) already mandates an end-of-run human read of the amended passages.
